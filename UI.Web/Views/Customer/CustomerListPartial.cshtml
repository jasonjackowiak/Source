@model SelectList

@Html.DropDownList("Existing Customers", (SelectList)Model)     

            @*@Html.DropDownList("Existing Customers", new SelectListItem() { Text = item.Name, Value = item.Name })*@

